import { test, expect } from "@playwright/test";
import type { MutationEnvelope, SnapshotEnvelope } from "../../src/headless.js";
test("dedicated worker renders, commits FIFO, fans out and destroys",async({page})=>{
  await page.addInitScript(()=>{Object.defineProperty(HTMLCanvasElement.prototype,"transferControlToOffscreen",{value:()=>{throw new Error("sabotaged")}});Object.defineProperty(window,"OffscreenCanvas",{value:undefined});});
  await page.goto("/test/browser/index.html");await page.evaluate(()=>window.ready);
  const result=await page.evaluate(async()=>{const api=window.api;const mutations:MutationEnvelope[]=[];const snapshots:SnapshotEnvelope[]=[];api.onMutations(e=>mutations.push(e));api.onSnapshots(e=>snapshots.push(e));const [a,b]=await Promise.all([api.dispatch({type:"node.add",nodeType:"fxnode.shader.value",position:{x:0,y:0}}),api.dispatch({type:"node.add",nodeType:"fxnode.shader.value",position:{x:1,y:1}})]);const before=snapshots.length;const snap=await api.snapshot();const querySilent=snapshots.length===before;const mirror=document.querySelector<HTMLCanvasElement>("#mirror");const copy=document.querySelector<HTMLCanvasElement>("#copy");const primary=document.querySelector<HTMLCanvasElement>("#primary");if(!mirror||!copy||!primary)throw new Error("Canvas missing");api.addMirror(mirror);await api.copyTo(copy);const pixel=(c:HTMLCanvasElement)=>Array.from(c.getContext("2d")!.getImageData(2,2,1,1).data);const saved=await api.save();await api.undo();await api.redo();api.destroy();let destroyed=false;try{await api.snapshot()}catch{destroyed=true}return{a,b,versions:mutations.map(e=>e.version),paired:mutations.every((e,i)=>snapshots[i]?.version===e.version),querySilent,pixels:[pixel(primary),pixel(mirror),pixel(copy)],catalog:saved.catalogVersion,nodes:snap.nodes.length,destroyed};});
  expect(result.a.version).toBe(1);expect(result.b.version).toBe(2);expect(result.versions).toEqual([1,2,3,4]);expect(result.paired).toBe(true);expect(result.querySilent).toBe(true);expect(result.catalog).toBe(3);expect(result.nodes).toBe(2);expect(result.destroyed).toBe(true);expect(result.pixels[0]).toEqual(result.pixels[1]);expect(result.pixels[0]).toEqual(result.pixels[2]);expect(result.pixels[0]?.[3]).toBe(255);
});
