import type { ColorPickerLayout, Rect } from "./types.js";
import type { Vec2 } from "../core/types.js";
export function layoutColorPicker(anchor:Rect,viewport:Vec2):ColorPickerLayout{const padding=10,gap=8,planeSize=176,strip=18,width=padding*2+planeSize+gap+strip+gap+strip,height=padding*2+planeSize;let x=anchor.x+anchor.width+8,y=anchor.y;if(x+width>viewport.x-8)x=anchor.x-width-8;if(y+height>viewport.y-8)y=viewport.y-height-8;x=Math.max(8,x);y=Math.max(8,y);return{bounds:{x,y,width,height},plane:{x:x+padding,y:y+padding,width:planeSize,height:planeSize},lightness:{x:x+padding+planeSize+gap,y:y+padding,width:strip,height:planeSize},alpha:{x:x+padding+planeSize+gap+strip+gap,y:y+padding,width:strip,height:planeSize}};}
